{
  "name": "tamil-heritage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "powershell -Command \"cd backend; .venv\\Scripts\\python.exe -m uvicorn app.main:app --port 8000 --reload\"",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run dev"
  }
}
